## 1. Extension scaffold

- [x] 1.1 Create the minimal TypeScript VS Code extension scaffold and required project metadata.
- [x] 1.2 Register a `Log Peak` panel view contribution that appears in the VS Code bottom panel.

## 2. Panel shell implementation

- [x] 2.1 Implement a `WebviewViewProvider` for the `Log Peak` panel shell.
- [x] 2.2 Render static seeded content that is primarily log-style with a small amount of code-like text.
- [x] 2.3 Apply theme-aligned styling so the panel visually fits the surrounding VS Code panel UI.

## 3. Verification

- [x] 3.1 Add or wire minimal repeatable tests for extension activation and panel registration behavior where practical for this phase.
- [x] 3.2 Verify manually in Extension Development Host that `Log Peak` appears in the bottom panel and matches the intended output/log direction.
