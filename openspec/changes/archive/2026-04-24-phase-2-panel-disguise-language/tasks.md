## 1. Loaded-state structure

- [x] 1.1 Update the loaded-state header and toolbar structure to match the Phase 2 panel disguise baseline.
- [x] 1.2 Replace the uniform `TXT` row prefix approach with partial mixed log/source tags while keeping the current line-number structure.

## 2. Visual language refinement

- [x] 2.1 Adjust panel CSS so loaded content reads as output/build-log first and only lightly borrows code-viewer rhythm.
- [x] 2.2 Tune row hover, separators, and color hierarchy so they stay restrained and do not make the panel feel like a reader UI.

## 3. Verification

- [x] 3.1 Add or update repeatable tests for any new row-tag generation or loaded-state rendering logic that becomes non-trivial in this phase.
- [x] 3.2 Verify manually in Extension Development Host that loaded TXT content now reads more like an output/build-log panel than a reader.
